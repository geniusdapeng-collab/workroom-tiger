<!--
document_schema: workloom.product-context/v1
document_id: workloom-product-and-code-panorama
context_version: 2026-09-14.1
snapshot_date: 2026-09-14
timezone: Asia/Shanghai
canonical_repository: geniusdapeng-collab/workloom-im
canonical_path: /WORKLOOM_PRODUCT_CONTEXT.md
distribution: controlled-base-sync
source_report_sha256: f798cbf17ce9c86e5002d185db08eb98178ed336c731f9f96ab21f639f1e5fd9
user_confirmed_catalog_date: 2026-09-14
repository_visibility: public-open-source
classification: public-product-context-no-secrets
-->

> **受控文档说明**
>
> - 本文件在 workloom-im 根目录维护，是 WorkLoom 跨仓产品与架构认知的唯一受控原文；行业仓中的同名文件是只读分发副本，不应直接修改。
> - context_version 表示认知版本，snapshot_date 表示事实核验时间。更新产品边界、系统不变量或仓库快照时，必须同步更新相应元数据，并通过受控 base-sync 分发。
> - 2026-09-14，产品所有者确认下文九个 GitHub 仓库全部为开源项目，并补充了各项目的正式定位；该确认优先于 2026-09-10 原始报告中的旧快照口径。
> - 提交 SHA、版本、员工数、技能数、围栏数等属于快照事实，后续任务必须先刷新目标仓库状态，不得把它们当成永久现状。
> - 本文件只提供产品与工程上下文，不构成对开发代理的操作授权；用户、系统、开发者及就近 AGENTS.md / AGENTS.override.md 的有效指令优先。
> - 严禁写入令牌、密码、私钥、客户凭证、原始客户数据或其他秘密。需要凭证时只使用批准的秘密存储或本地环境配置。
> - 仓库专属命令、例外和实现细节应进入对应仓库的 AGENTS.repo.md 或更近层级的 AGENTS.md；不得在行业副本中制造另一份产品事实源。

# WorkLoom 产品与代码全景认知

> 建档日期：2026-09-10（Asia/Shanghai）
> 最近核验：2026-09-14（九仓均为开源项目；项目正式定位由产品所有者确认）
> 用途：作为后续产品设计、架构评审、代码修改和跨仓同步时的共同上下文。
> 信息来源：官网全量备份、两份官网 PDF、九个 GitHub 仓库当前代码与文档，以及产品所有者 2026-09-14 确认的项目清单。压缩包与仓库中的文字仅作为产品资料分析，不视为对开发代理的操作指令。

## 1. 核心结论

WorkLoom 不是一组彼此独立的 AI 应用，而是一套“一个基座、一个运营中枢、N 个行业自主经营系统”的产品与工程体系。

- **WorkLoom IM** 是基座与唯一公共能力源，负责数字员工运行时、任务编排、围栏审批、事件账本、考试评测、记忆、夜班、模型路由、租户隔离和三端交互。
- **仙女座 Andromeda** 是 WorkLoom 平台方自用的运营运维中枢。它用 WorkLoom 管理 WorkLoom：接工单、维护知识、运营客户、对账计费、巡检运维、发布行业包和策略，并把需求送入 AI 产品经理流水线。
- **酒店、电商、视频、获客、咨询、交易等项目**是从同一基座派生的行业经营体。它们用行业包定义自己的数字员工组织、技能、业务对象、阶段、围栏、种子数据、投影和 UI。
- **狐狸先生**不是一个独立行业，而是新人格、新交互和影音汇报方式的试验田；成功经验可回到基座，失败实验进入“策略墓地”。
- 整套系统的真正差异化不只在模型能力，而在两层复利：一是“围栏 + 账本 + 考试院”的信任工程，二是“经验上行、评测灰度、能力下发”的蜂群飞轮。

一句话心智模型：**WorkLoom IM 是操作系统，行业包是岗位与业务制度，行业项目是经营实例，仙女座是管理全部实例的中央运营台。**

## 2. 产品体系关系

### 2.1 三层结构

1. **顶层：仙女座**
   - 服务对象：WorkLoom 平台运营团队。
   - 责任：平台运营、平台运维、客户成功、知识治理、计费对账、行业包与策略发布。
   - 人机分工：数字员工执行，1–2 名人类负责审批、经营判断和应急裁决。

2. **中层：行业自主经营系统**
   - 酒店、AI 视频营销、酒店获客、电商、股票交易试验、咨询、狐狸试验田及未来行业。
   - 每个系统共享基座，但拥有自己的组织结构、业务语义、围栏和交付界面。

3. **底层：企业客户及其客户**
   - WorkLoom 服务企业；企业再通过数字员工服务住客、消费者、投资主体或企业客户。
   - 形成 ToB→C 或 ToB→B 的实际经营链路，而不止是内部办公提效。

### 2.2 能力与信息的双向流动

- **向下分发**：基座升级、行业包、技能、策略、模型配置、灰度版本和安全规则。
- **向上回流**：工单、需求、效果指标、异常类型、失败原因及经营反馈。
- **数据边界**：客户原始经营数据不应离库；进入中枢和蜂群的应是脱敏、白名单化、可审计的元数据或经验产物。
- **平台工程边界**：`platform-ops` 属于仙女座，不应随基座同步到行业客户仓库。

## 3. WorkLoom IM 基座

### 3.1 产品职责

WorkLoom IM 把“AI 能否可靠顶岗”拆成一组可工程化问题：

- 数字员工是否有实名身份、岗位职责、可调用技能和工作范围；
- 每一步动作是否经过围栏判断；
- 涉钱、涉数据或其他高风险动作是否由人审批；
- 工作过程是否形成可回放、防抵赖的事件与回执；
- 模型、提示词、技能或策略升级后是否重新考试；
- 长任务中断后能否恢复，而不是从头重做；
- 不同租户、行业和客户个性化需求是否能够隔离演进。

基座默认叠加 AI 产品经理团队作为可体验示例，并内置 `bundles/hotel/` 行业 Bundle；围栏 DSL、技能保鲜环、L2 编排、夜班、模型路由、五元事件与 RLS 隔离等构成 README 所列的 10 项 AI 自动化能力。用户可以先体验“AI 自主经营团队”，再一键清理示例，并通过配置引导建立自己的行业垂直版；示例业务不是公共运行时的第二事实源。

### 3.2 技术结构

- pnpm monorepo；Node.js 24+、pnpm 10、PostgreSQL 17 + pgvector。
- 服务端：Hono + tRPC v11，包含健康检查、tRPC 入口、C 端服务网关以及可选的技能分发/回流入口。
- Web：React 19.2、React Router 8.3、React Query，并使用 Three Fiber、Pixi/Live2D 等交互能力。
- 三端产品面：PC B 端、移动 B 端、移动 C 端。
- 核心包：`@workloom/base`、`@workloom/runtime`、`@workloom/db`、`@workloom/shared`。
- 基座模块覆盖 accounts、audit-core、bundles、captain、computer-use、dev-bridge、eval-core、event-bus、evolve、fence-engine、IM、inspection、model-router、night-shift、overlay、review-console、service-*、skill-ops、tenancy、wizard、workdata 等。

### 3.3 关键执行链路

典型链路为：

`用户输入 → 意图路由 → 预设/班组装配 → Quest/TaskGraph → 每步围栏判断 → 自动执行或人工审批 → 回执 → 事件账本 → 记忆/评测/投影`

主要设计含义：

- 意图会被分为问答、单 Agent 和多步骤 Quest；路由可由模型完成，也有规则兜底与澄清分支。
- 班组装配要求人员档案、阶段和目标齐全；有效权限是预设与已安装技能围栏绑定的并集，而不是模型自行决定。
- Quest 的每一步都先走围栏，再执行、评审或阻断；没有回执就不能宣称完成。
- 步骤 ID 与事件 ID 支持重放和断点续做。

### 3.4 WorkData 与事件账本

WorkData 事件以五元结构记录业务事实：

- `who`：谁做的；
- `context`：在什么租户、会话、任务与环境中；
- `object`：对什么业务对象；
- `decision`：做了什么判断或动作；
- `rule_impact`：命中了什么规则、权限或风险影响。

同时保存回执、模型调用轨迹、关联关系、哈希和时间戳。事件采用只增不改思路，配合全局顺序、哈希链、PII 深度脱敏、租户 RLS 与重放前缀，承担黑匣子、审计证据和经验素材三种职责。

### 3.5 围栏与审批

- 服务网关的核心顺序是：**权限检查 → PII 脱敏 → 高风险审批校验 → 执行**。
- 围栏判定是确定性纯函数：对象/动作匹配后运行规则 DSL；冲突时 `block > review > auto`。
- 读操作无命中可自动通过；写操作无命中使用更保守的默认策略；规则求值异常按阻断处理。
- 高风险审批必须存在、未过期、内容匹配且状态已批准，不能被“自动通过”绕过。
- 审批有批准、修改、驳回三种明确手势；驳回必须给原因，修改必须给新值；跨渠道操作需要幂等。
- 围栏从草稿到生效需要近期事件干跑验证和审批；对象级锁避免并发更新。

### 3.6 考试院、错题本与持续进化

- 数字员工在上岗前考试；红线题失败应一票否决。
- 失败、驳回和人工修改不只是日志，也应自动沉淀为评测用例和“错题本”。
- 模型、技能、策略和行业包的新版本先考试，再灰度；能力退化则回炉或回滚。
- 这条链路是官网近期路线图的重点：`失败/中断 → 自动归因 → 评测用例 → 新版本验证 → 灰度上线`。

### 3.7 个性化覆盖层

覆盖层包含 persona、品牌、知识库、班组、阈值、技能和围栏等资产，遵循：

- 基座与行业默认资产不被客户直接改写；
- 客户配置以覆盖层叠加；
- 围栏只能收紧，不能越权放宽基座红线；
- 生命周期为草稿、考试、灰度、正式；
- 可生成快照和回滚；基座升级后 rebase 会区分兼容、可自动回退和需要人工决策。

### 3.8 事件总线与运行可靠性

- 支持内存、NATS 和 Redis 适配器；开发环境可使用内存，生产形态偏向持久化后端。
- 设计目标包括 ack、重放、延时任务和至少一次投递；同步本地外观与后台持久化镜像兼容旧调用链。
- 仙女座进一步叠加 outbox、投影器、限流、熔断、慢车道、租约、漏触发对账、SLO 燃烧率、热更新灰度、主备 fencing 与容量模型。

## 4. 仙女座与 AI 产品经理流水线

仙女座是平台自用经营体，不是另一个客户行业版。它既包含普通行业项目同样的数字员工/技能/围栏结构，也包含平台专属工程运维能力。

### 4.1 组织与职责

- 17 名平台数字员工，分为 7 个班组：工单/FAE、知识管理、客户成功、计费账务、平台运维、行业包运营、策略运营。
- 需求通过客户、运营、内部三类入口进入，以工单为账本，以技术支持为桥梁，以知识库为弹药。
- 运营机制以自生长为引擎：工单、知识、运行证据、评测与复盘持续转化为可审计、可灰度、可回滚的能力改进。
- 关键经营指标包括审批及时性、自动化率、工单 SLA、客户健康、续费预警、积分三池对账和交付回访。

### 4.2 AI-PM 流水线

- 由 14 名数字员工组成，从需求澄清、范围与验收到架构、前后端、测试、评测、安全、发布和复盘。
- 需求先经过澄清四问与确认单，再进入研发；关键节点需要及时向客户回传。
- 变更经过人审立项、开发、评测、灰度、发布、验收和第 3 天回访。
- 这不是“自动写代码”单点能力，而是把产品研发本身做成一条受围栏、评测和发布闸门约束的自主流水线。

### 4.3 平台专属能力

Andromeda 的正式产品差异资产集中在 `bundles/platform/`（platform-bundle）。平台侧的 `platform-ops`、事件流/outbox/投影、积分、异常、SLA、推送/接入网关、限流熔断、租约看门狗、热更新、安全冻结、主备、容量规划、账户运营与 TOTP 等能力均属于运营中枢边界，不得下发到客户行业仓。当前基座仍出现同名目录的代码漂移，见 §9.1，不能因此改变产品所有者确认的责任边界。

## 5. 九个仓库的定位与代码理解

九个仓库均位于 `geniusdapeng-collab` 且全部开源。以下提交是 2026-09-14 重新读取远端 `main` 后的 HEAD 快照，后续继续开发时仍应重新核对。

| 仓库 | 快照 | 定位 | 代码层面的主要差异 |
|---|---:|---|---|
| [`workloom-im`](https://github.com/geniusdapeng-collab/workloom-im) | `d81e380` | 公共基座、默认 AI-PM 示例版 | 公共 runtime/base/db/shared、三端应用、`bundles/hotel/` 示例与 10 项 AI 自动化能力；支持体验后清理示例并按配置引导建立行业版 |
| [`workroom-andromeda`](https://github.com/geniusdapeng-collab/workroom-andromeda) | `8734db7` | 平台运营运维中枢 | 用 AI 运营运维 AI；正式差异资产以 `bundles/platform/` 的 platform-bundle 为中心，执行交给数字员工、裁决永远在人 |
| [`workloom-hotel`](https://github.com/geniusdapeng-collab/workloom-hotel) | `69b99d6` | 酒店自主经营系统 | 基于 WorkLoom IM v1.5.0 通用底座的行业配置；面向低星单体酒店、民宿和无人酒店，联通 PC、店主端与住客端 |
| [`hyperreality-system`](https://github.com/geniusdapeng-collab/hyperreality-system) | `92df7fe` | AI 视频社媒营销经营体 | Multi-Agent 视频生产线、25/30 字段镜头卡、导演前置评审、定妆锚定、事实闸机、组织记忆与自动化投放闭环 |
| [`workloom`](https://github.com/geniusdapeng-collab/workloom) | `cdc2b43` | 酒店获客复合系统 | 叠加酒店经营、AI 视频、GEO 与流量转化，按“意图洞察—双域触达—四路承接—线索转化—归因复盘”闭环经营 |
| [`panda-cineforge`](https://github.com/geniusdapeng-collab/panda-cineforge) | `65f54f0` | 电商自主经营系统 | 82 人数字军团覆盖选品、刊登、调价、广告、客服、对账和夜班；面向多平台腰部卖家与小团队 |
| [`workroom-tiger`](https://github.com/geniusdapeng-collab/workroom-tiger) | `5b34804` | 全球资产管理高风险试验 | 37 人数字投研班组、21 环节管线、六层决策栈与 WorkLoom 治理壳；覆盖美/A/港研究，当前仅模拟盘 |
| [`workroom-eagle`](https://github.com/geniusdapeng-collab/workroom-eagle) | `0733531` | 鹰眼 AI 咨询管理系统 | 一个咨询师 + 22 名数字员工 + 199 个技能；围绕“一企一档”持续积累可归因的上下文与判断力 |
| [`workroom-fox`](https://github.com/geniusdapeng-collab/workroom-fox) | `f1cebe7` | “懂汇报的狐狸先生”试验田 | 同源酒店版，验证 3D 数字职场/汇报舞台、语音、字幕、导演运镜、视线感知与多人多店协作体验 |

### 5.1 WorkLoom Hotel

- 从 `audit-only → shadow → managed` 逐步取得经营权，适合低星小酒店、民宿与无人酒店等人力紧张场景。
- 目标门店包括 20–50 间房的单体酒店、5–20 间房的民宿和无人酒店；AI 前厅、客房与收益协同工作，默认演示门店为“云栖酒店”。
- 酒店审计引擎覆盖价格、渠道、库存、口碑与安全；包含住客/房态/工单/会员等业务对象及数字孪生界面。
- 当前 manifest 与 README 徽章存在版本/技能数量漂移，开发时以代码与 manifest 为准并同步文档。

### 5.2 WorkRoom Fox

- 目前并未形成独立行业语义，仍继承酒店版的大部分组织与能力。
- 核心演进正式命名为 M1“视听觉醒”，探索“数字员工如何被看见和汇报”：3D 数字职场与汇报舞台、骨骼动画、三层音效、WebAudio、语音播报、新闻台字幕、导演运镜和视线感知等。
- 账号实验覆盖门店团队四角色、一人多店统一待办与伙伴协作授权；这些酒店实例语义不能被误判为新的公共基座默认值。
- 应保持实验边界：验证成功的通用交互能力才进入基座，不把试验语义直接污染公共层。

### 5.3 HyperReality

- 生产与运营数字员工共同完成选题、脚本、生成、混剪、审核、发布、监测和复盘。
- 镜头语言以 25 字段核心结构及 30 字段扩展口径组织，配合导演评审前置拦截、定妆照锚定、事实红线闸机和组织记忆，减少“抽卡式”不确定性。
- 独有的资产、渲染、发布与成本模块使其更接近内容生产工厂，而非单个视频生成工具。
- 外部平台自动发布是高风险动作，需要账号隔离、平台适配、频控、回执与失败重试。

### 5.4 WorkLoom 获客系统

- 不是单一垂直版，而是三个系统的组合编排：酒店经营提供业务落点，视频系统生产并投放内容，GEO 系统让品牌在搜索与 AI 问答中被发现。
- 主线是流量和收入闭环：意图发现、内容/搜索触达、四渠道接待、线索评分与成交、全渠道归因。
- 其复杂度来自跨包共享对象、线索身份和归因数据，需要特别注意边界与幂等。

### 5.5 Panda Cineforge

- 数字员工和技能数量在当前体系中最大，覆盖选品、定价、广告、客服、售后、库存、履约、合规和复盘。
- 主要服务同时经营四五个平台的腰部卖家、一人管理多店的小团队与跨境夫妻店；行业测算中，选品、刊登、调价、广告、客服、对账、库存和履约等“八大流失点”通常侵蚀卖家每年 3–8% 的营收，产品以减少这些可归因损失为经营口径。
- 连接器注册与 mock profile 是核心基础设施；开发时必须清楚区分真实连接和体验模式，不能让演示回执伪装成外部平台成功。
- 围栏既有平台基准，也有客户补丁，正好体现“默认制度 + 只可收紧的个性化层”。

### 5.6 Eagle

- 目标是放大顾问产能，而不是取消所有顾问活动。
- 产品组织模型是“一名咨询师 + 22 名数字员工 + 199 个专业技能”，以“一企一档”持续积累上下文；正式信任公式为“信任 = 判断力 × 上下文深度 × 可归因性”。
- 初次会面、访谈、最终诊断等高信任时刻被明确保留给人；AI 负责资料整理、研究、分析、方案草拟、知识复用、异常提醒和复盘。
- 与基座共同路径高度一致，行业差异主要集中在咨询 bundle、种子和少量界面/服务配置，属于较标准的行业派生范式。

### 5.7 Tiger

- 架构与其他项目不同：Python 负责市场、策略、回测、仓位与风险计算；TypeScript WorkLoom 层放在 `governance/` 中，负责治理而非计算 alpha。
- 一名 AI 基金经理统筹 37 人数字投研班组，以 21 环节管线和六层决策栈覆盖美股、A 股与港股研究；系统按 24 小时连续运行目标设计，但持续运行不等于获准自动实盘。
- 决策采用六层结构：市场状态、MRS、行业、产业链、TSS、风险；当前路线图完成 S1–S6，处于至少三个月的纸面交易验证期。语义判断归 LLM、数值计算归确定性规则、闸门必须确定。
- `config.py` 是策略参数单一事实源；围栏应从配置生成并可审计，避免 Python 与治理包出现双重真相。
- 真实券商执行尚未启动，当前结果不构成投资建议；任何后续开发都不能把模拟结果表述为真实交易能力。

## 6. 基座同步与仓库治理

基座通过同步配置向子仓分发公共代码，同时保护行业差异：

- 同步公共 packages、服务端核心、桌面端、Web 公共库、脚本和基础配置。
- 默认排除行业 bundles、演示/种子、行业文档、特定业务页面、行业服务、迁移和行业技能等。
- `package.json` 使用锚点式合并，保留子仓额外依赖与脚本。
- 污染守卫会阻断行业包、种子、hotel/ai-pm/ecommerce、`platform-ops` 等越界内容，并限制异常大规模改动。
- Tiger 使用 `governance/` 路径前缀；Andromeda 额外排除 `platform-ops/**`，因为平台工程能力应只在目标仓维护。

后续跨仓开发默认顺序：

1. 先判断能力属于公共基座、平台中枢还是行业包。
2. 公共机制优先在 `workloom-im` 实现并通过同步下发。
3. 行业语义仅在对应 bundle/子仓实现。
4. 平台运营工程仅在 Andromeda；不得通过 base-sync 扩散。
5. 同步后逐仓运行类型检查、测试、围栏/评测和打包门禁，不能只看文件复制成功。

## 7. 官网产品规划的近期重点

### P0

- 租户个性化覆盖层：已交付；
- L1 自然语言需求入口：已交付；
- 事件总线持久化与可靠重放；
- 长任务中断/恢复看板；
- 失败轨迹自动转评测用例，形成错题本闭环。

### P1

- 反向访谈与更完整的需求澄清；
- L2 多角色编排；
- 投影快照和事件 Schema Registry；
- TaskGraph；
- 蜂群经验上传白名单与匿名化；
- 任务经济学、吞吐与留存平滑度指标。

### P2

- 从共性中挖掘个性化需求；
- 蜂群匿名化和经验数据库；
- 完整的经验上行—评测—灰度—下发飞轮；
- 与行业伙伴共建更深的业务模型。

## 8. 后续开发必须守住的系统不变量

1. **租户隔离**：数据库 RLS、运行时上下文和缓存键都必须带租户边界。
2. **只增不改**：经营事件、审批证据和关键回执不可静默覆盖；修正应产生新事件。
3. **先围栏后动作**：任何入口和子调用都不能绕过统一网关。
4. **高风险必须人审**：涉钱、涉敏感数据、群发、外部发布、交易与破坏性变更不能被默认自动化。
5. **无回执不算完成**：UI 状态、审计和任务恢复都以真实回执为准。
6. **客户原始数据不上行**：蜂群只接收经过白名单、脱敏与审核的元数据/经验。
7. **覆盖层只能收紧**：客户个性化不能突破平台不可变红线。
8. **平台工程不出仓**：`platform-ops` 与平台账户/运维秘密只属于 Andromeda。
9. **演示与真实能力分明**：mock、模拟回执和体验模式必须在界面与事件中明确标识。
10. **版本变更先考试再灰度**：模型、技能、行业包、策略和热更新都要可回滚。
11. **秘密不进代码或上下文文件**：令牌、密钥和客户凭证只应进入受控秘密存储或本地环境配置。
12. **高风险金融边界**：Tiger 在明确授权、合规设计和长期纸面验证完成前，不接真实资金与自动实盘。

## 9. 已发现、需要优先核实的漂移

### 9.1 `workloom-im` 的角色污染迹象

2026-09-14 复核时，`workloom-im` 的 HEAD README 已恢复为“WorkLoom 织元 · DeepSeek Harness 企业级 Agent IM”，此前的 README 角色漂移已修复；但基座当前仍同时存在 `bundles/platform/` 和 `platform-ops/`。产品所有者确认 Andromeda 才是运营中枢，正式差异资产集中于其 `bundles/platform/`，而 `platform-ops/README.md` 与同步污染守卫也把平台运维工程限定在 Andromeda。

这不能仅凭目录名直接判定为代码错误，但仍高度像尚未清理的同步污染、内置演示资产与平台专属工程边界混杂。开始大规模开发前，需要确认：

- 基座中的 platform bundle 是否只是可清理的内置演示资产，还是应该完全移出；
- `platform-ops` 是否需要从主仓历史和当前工作树中拆出；
- 拆分前先建立迁移、同步污染守卫和回滚方案，不能用直接删除替代产品边界确认。

### 9.2 README 与 manifest 漂移

酒店 README 的版本徽章/技能数量与当前 manifest 不一致；其他仓库也存在文档里的产品口径领先或落后于代码的问题。建议以后把版本、员工数、技能数、围栏数等统计由 manifest 自动生成，减少手工维护。

### 9.3 基座出现试验田语义

基座文档中出现面向狐狸先生 M1 的技术设计内容。需要确认它是公共交互能力设计还是误入主仓的试验项目文档，并据此决定保留、抽象或迁移。

## 10. 后续协作的默认判断框架

收到新需求时，先回答四个问题：

1. 它改变的是公共运行机制、平台运营机制，还是某个行业的业务语义？
2. 它会不会改变租户边界、围栏、审批、事件与回执这五个信任要素？
3. 它应该进入基座、Andromeda，还是一个/多个行业包；如何同步和防污染？
4. 它如何被测试、进入考试院、灰度、回滚，并把真实失败变成新的评测资产？

只有这四项回答清楚，才进入实现。

## 11. 凭证与“记忆”说明

- 本文没有保存任何 GitHub Personal Access Token、密钥或客户凭证。
- 九个仓库均为产品所有者确认的开源项目；读取和继承公共上下文不需要私人令牌。
- 九仓根目录均应持有 `AGENTS.md` 与本文：从任一仓库启动的新开发任务会先获得共同产品关系、开发边界和本文入口，并在实质性 WorkLoom 工作前完整读取本文。
- `workloom-im` 是本文唯一受控原文；其他仓只接收校验过的副本。新行业仓必须通过 `sync/adopt.sh` 接入，若直接从基座派生则天然携带本文件与根级指引。
- 未来任务应把本文作为可版本化的持久项目上下文，而不是模型的隐式跨对话记忆；实际开发前仍应检查目标仓最新提交和仓内规则，避免把 2026-09-14 快照当成永久现状。
